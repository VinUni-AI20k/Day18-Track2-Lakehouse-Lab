The anti-pattern our team is most at risk of is **small files from streaming micro-batches** (slide §6, Job 1). Our LLM-observability path looks like NB6: many small, correct commits. We measured 200 files at 51.5 KB (production target 128–512 MB). Compaction cut 200 → 11 files (18×). At 50k scans/day that is $4.00 vs $0.08 in S3 GETs. NB2 showed the query cost: 9.9× speedup, and Z-ORDER isolated `user_id=4242` to 1 of 55 files (55× prune).

What the lab changed my mind about is pairing Job 3 with Job 4. Delta `VACUUM` reclaimed 16.1 MB of *tombstoned* files but left three planted uncommitted orphans the log cannot see. Iceberg `expire_snapshots` dropped 20 → 3 snapshots and deleted **zero** avro files; metadata grew 332 → 340 KB until we swept stranded manifest lists. If we “turn on VACUUM” and stop, the bill will not drop. Compaction is the first cron; the Disk \ Log orphan sweep is the one we must not skip.
