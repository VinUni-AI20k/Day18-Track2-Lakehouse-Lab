# Lab 18 Reflection

> Giới hạn: tối đa 200 từ. Thay các phần `<...>` bằng nội dung của bạn và xóa dòng hướng dẫn này trước khi nộp.

**Anti-pattern có rủi ro cao nhất:** `<Tên anti-pattern trong slide>`

Dữ liệu của nhóm tôi `<mô tả ngắn loại dữ liệu và quy trình hiện tại>`. Anti-pattern này dễ xảy ra vì `<nguyên nhân cụ thể liên quan đến cách ghi, tổ chức hoặc bảo trì dữ liệu>`.

Nếu không xử lý, hệ thống có thể `<hậu quả có thể đo được: truy vấn chậm, chi phí lưu trữ tăng, dữ liệu thiếu nhất quán hoặc khó truy vết>`.

Nhóm sẽ giảm rủi ro bằng cách `<biện pháp cụ thể>`. Chúng tôi sẽ theo dõi `<chỉ số như số file, pruning ratio, thời gian truy vấn hoặc dung lượng lưu trữ>` để xác nhận giải pháp có hiệu quả.
