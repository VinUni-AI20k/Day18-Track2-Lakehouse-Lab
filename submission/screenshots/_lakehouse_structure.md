# Lab 18 — Screenshots: Lakehouse Structure

## _lakehouse/ top-level (after make run-all)

```
_lakehouse/
  blobs/          (200 binary blob files — NB7 multimodal)
  bronze/         (3 parquet — NB4 medallion + NB7/NB8 data)
  gold/           (17 parquet — NB4 Gold, NB8 agent performance)
  iceberg/        (34 parquet — NB5/NB6 Iceberg tables)
  scratch/        (353 parquet — NB1-NB3, NB6 maintenance working tables)
  silver/         (20 parquet — NB4 Silver, NB8 governed corpus)
```

## Delta transaction log (Bronze llm_calls_raw)

File: `_lakehouse/bronze/llm_calls_raw/_delta_log/00000000000000000000.json`

```
Line 1 [commitInfo]:  operation=WRITE,  rows=200,000
Line 2 [protocol]:     reader=1,  writer=2
Line 3 [metaData]:     format=parquet,  3 schema fields
Line 4 [add]:          part-00000-....snappy.parquet,  13,988,345 bytes
```

## NB1 users_delta — 2 log commits

```
_lakehouse/scratch/users_delta/_delta_log/
  00000000000000000000.json  → WRITE (v0)
  00000000000000000001.json  → WRITE (v1 — schema_mode="merge" added tier)
```

## Medallion — Bronze / Silver / Gold on disk

```
_lakehouse/bronze/llm_calls_raw/         (200K rows, raw JSON)
_lakehouse/silver/llm_calls/             (deduped, typed, partitioned by date)
_lakehouse/gold/llm_daily_metrics/       (p50/p95/cost per date×model, Z-ordered)
  date=2026-04-01/  date=2026-04-02/  ...  date=2026-04-07/   (7 days × 3 models)
```

## NB5 Iceberg — metadata tree

```
_lakehouse/iceberg/nb5/lake.llm_events/
  metadata/
    v0.metadata.json  ...  v10.metadata.json   (11 snapshots)
    snap-<id>.avro                       (manifest lists)
  data/
    00000-...parquet  ...  00009-...parquet    (10 daily batches)
```

## NB8 — 4 Art. 10 provenance buckets

```
_lakehouse/silver/training_corpus_governed/
  provenance_bucket=licensed/
  provenance_bucket=public_domain/
  provenance_bucket=scraped_optout_checked/
  provenance_bucket=synthetic/
```

Generated by Khoa (2026-08-18) — all 8 notebooks PASSED (make run-all).
