# Submission screenshots

Đặt ảnh minh chứng của bài lab trong thư mục này. Tên file gợi ý:

- `cp01_pass.png` — khối `PASS` và `NB1 complete.`
- `cp01_delta_log.png` — cây thư mục bảng và nội dung một file `_delta_log/*.json`.
- `cp02_optimize_metrics.png` — số file trước/sau, speedup hoặc pruning ratio.
- `cp03_time_travel.png` đến `cp08_provenance.png` — kết quả chính của từng checkpoint.

Trước khi nộp, kiểm tra ảnh đọc được, không chứa token/mật khẩu và khớp với output đã lưu trong notebook.
