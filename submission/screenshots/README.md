# Screenshots

Rubric yêu cầu **ít nhất một** trong hai:

* **Đường Spark** — MinIO console (http://localhost:9001) hiển thị
  `_delta_log/` và bố cục bucket `lakehouse / bronze / silver / gold`
* **Đường lightweight** — `tree _lakehouse/` kèm nội dung một file
  `_delta_log/*.json`

Đặt ảnh vào thư mục này. Xoá file README này nếu muốn.
