# Lightweight core runtime for the default path — pure Python, no JVM, no Docker.
# This keeps `make setup` fast; notebook UI tools are installed lazily by `make lab`.
deltalake>=0.21,<1.0
duckdb>=1.1,<2.0
polars>=1.13.2,<2.0   # 1.13.1 was yanked
pyarrow>=17,<22
